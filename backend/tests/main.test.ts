import {describe, expect, it} from "vitest";

describe('Temporary test', () => {
    it('Some test', () => {
        const stringer = "some string"
        expect(stringer).toEqual("some string")
    })
})